# Copy this file to `.env.local` and fill in values.
#
#   cp env.example .env.local
#

## Shopify (Storefront API)
# Your store domain, e.g. "your-store.myshopify.com"
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=

# Storefront API access token
NEXT_PUBLIC_SHOPIFY_STOREFRONT_TOKEN=

## Shopify Admin (optional)
SHOPIFY_ADMIN_ACCESS_TOKEN=

## NextAuth (optional)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

## Pre-launch internal access (client-side)
# Set to a non-empty value to enable the "Team Access" password prompt on the countdown.
NEXT_PUBLIC_LAUNCH_ACCESS_PASSWORD=

# Countdown gate (client-side)
# Enable the pre-launch countdown overlay.
NEXT_PUBLIC_LAUNCH_COUNTDOWN_ENABLED=false

# Quick test mode (client-side)
# When enabled, the countdown will run for the specified duration and then trigger the reveal/unlock sequence.
NEXT_PUBLIC_LAUNCH_TEST_MODE=false
NEXT_PUBLIC_LAUNCH_TEST_DURATION_SECONDS=10

# Bump this value (and redeploy) to invalidate all previously granted Team Access.
# Example: 0 -> 1 (or 2025-12-22)
NEXT_PUBLIC_LAUNCH_ACCESS_RESET_TOKEN=0

## Customer Accounts / OIDC (optional)
CUSTOMER_ACCOUNTS_ENABLED=false
SESSION_SECRET=
SHOPIFY_CA_CLIENT_ID=
SHOPIFY_CA_AUTH_URL=
SHOPIFY_CA_TOKEN_URL=
SHOPIFY_CA_LOGOUT_URL=
SHOPIFY_CA_REDIRECT_URI=
SHOPIFY_CA_REDIRECT_URI_LOCAL=
SHOPIFY_CA_CUSTOMER_API_URL=
SHOPIFY_CA_SCOPES=openid email customer-account-api:full

## Forms -> Google Sheets (service account)
# Spreadsheet ID (the part after `/d/` in the URL).
GOOGLE_SHEETS_SPREADSHEET_ID=

# Service account email (e.g. "forms-writer@your-project.iam.gserviceaccount.com")
GOOGLE_SERVICE_ACCOUNT_EMAIL=

# IMPORTANT: keep the \n sequences (no real newlines).
# Wrap in quotes if your shell/env loader requires it.
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n"
